import React from 'react';
import renderer from 'react-test-renderer';
import { render, screen, fireEvent } from '@testing-library/react';
import {} from '';

//  テストテンプレート コメントアウト解除して使う
// describe('コンポーネント名', () => {
//   it('ユニットテスト名', () => {
//     ここに実行して欲しいテストを記述します。
//   });

//   it('スナップショットテスト', () => {
//     const props = {
//       onClick: jest.fn
//     }

//     const tree = renderer
//       .create(<コンポーネント名 {...props} />)
//       .toJSON();
//     expect(tree).toMatchSnapshot();
//   });
// });
